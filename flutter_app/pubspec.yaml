name: one_stop_house_builder
description: Vendor-first marketplace MVP (Vendor registration flow)
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  http: ^1.5.0
  shared_preferences: ^2.2.3
  uuid: ^4.4.0
  razorpay_flutter: ^1.3.6
  # ⬇️ Firebase
  firebase_core: ^3.4.0
  cloud_firestore: ^5.4.4
  firebase_auth: ^5.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
